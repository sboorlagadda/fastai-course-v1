# fast.ai v1 course

Course notebooks are extracted into its own repo and updated accordingly to work with `fastai 0.7.0` library and specifically modified to work with Google Colab. This provides an easy way for anyone to follow the existing course videos and play with notebooks.

Use `setup.ipynb` notebook to setup colab environment.